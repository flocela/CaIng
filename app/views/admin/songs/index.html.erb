<h2>Songs</h2>
<% @songs.each do |song| %>
  <%= song.new_work_title %>
  <%= link_to 'Delete', "songs/#{song.id}", method: :delete %> 
<% end %>
<%= link_to 'Sign Out', destroy_admin_session_path, method: :delete %>

